import { useReducerState } from './context';

export default function Modal() {
  const modal = useReducerState().modal;

  // if (modal) {
  //   return (

  //   )
  // } else {
  //   return <></>;
  // }
}
